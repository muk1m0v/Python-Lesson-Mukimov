# 🎮 Python: Саёҳати қаҳрамон / Приключение героя / Hero's Adventure — 25 задач

## 📌 How to use / Как пользоваться / Тарзи истифода

- **EN:** Solve the tasks **in order** — this is one connected story about a game hero. Each task reuses the answer or idea from the previous one (see the 🔗 note), so by Task 25 you build a whole mini-program. Read input from the console and print the result **exactly** as shown in the examples.
- **RU:** Решайте задачи **по порядку** — это одна связанная история про героя игры. Каждая задача использует ответ или идею предыдущей (см. пометку 🔗), и к задаче 25 вы соберёте целую мини-программу. Считывайте данные с консоли и выводите результат **точно** так, как в примерах.
- **TJ:** Масъалаҳоро **аз рӯи навбат** ҳал кунед — ин як ҳикояи пайваст дар бораи қаҳрамони бозӣ аст. Ҳар масъала ҷавоб ё идеяи масъалаи қаблиро истифода мебарад (ба тавзеҳи 🔗 нигаред), ва то масъалаи 25 шумо як барномаи хурдро месозед. Маълумотро аз консол хонед ва натиҷаро **маҳз** мисли намунаҳо чоп кунед.

> 🟢 Осон / Лёгкая / Easy · 🟡 Миёна / Средняя / Medium · 🔴 Мушкил / Сложная / Hard

---

# Task 1: Welcome Screen 🟢

## English Description

Print a welcome message for the game to the console.

- 🔗 This is the **start of the chain** — the next tasks build on it.

## Описание задачи (на русском)

Выведите на консоль приветствие игры.

- 🔗 Это **начало цепочки** — следующие задачи опираются на неё.

## Тавсифи масъала (бо тоҷикӣ)

Ба консол паёми хушомадгӯии бозиро чоп кунед.

- 🔗 Ин **оғози занҷир** аст — масъалаҳои навбатӣ ба он такя мекунанд.

### Example:
```
Output: Хуш омадед ба бозии «Саёҳати қаҳрамон»!
```

---

# Task 2: Title Screen 🟢

## English Description

Using **three** `print` commands, build a title screen for the game.

- 🔗 Builds on the `print` from **Task 1**.

## Описание задачи (на русском)

С помощью **трёх** команд `print` создайте заставку игры.

- 🔗 Опирается на `print` из **задачи 1**.

## Тавсифи масъала (бо тоҷикӣ)

Бо **се** фармони `print` унвони бозиро созед.

- 🔗 Ба `print`-и **масъалаи 1** такя мекунад.

### Example:
```
Output:
========================
   САЁҲАТИ ҚАҲРАМОН
========================
```

---

# Task 3: Hero's Name 🟢

## English Description

Store the hero's name in a **variable** and print it.

- 🔗 Your **first variable** — you will reuse it in the next tasks.

## Описание задачи (на русском)

Сохраните имя героя в **переменной** и выведите его.

- 🔗 Ваша **первая переменная** — дальше вы будете её использовать.

## Тавсифи масъала (бо тоҷикӣ)

Номи қаҳрамонро дар **тағйирёбанда** нигоҳ доред ва онро чоп кунед.

- 🔗 **Аввалин тағйирёбандаи** шумо — минбаъд онро истифода мебаред.

### Example:
```
Output: Артур
```

---

# Task 4: Introduction 🟢

## English Description

Using the name variable, print the sentence: `Our hero is named <name>!`

- Use an f-string, e.g. `f"... {name} ..."`.
- 🔗 Reuse the `name` variable from **Task 3**.

## Описание задачи (на русском)

Используя переменную с именем, выведите фразу: `Нашего героя зовут <имя>!`

- Используйте f-строку, например `f"... {name} ..."`.
- 🔗 Используйте переменную `name` из **задачи 3**.

## Тавсифи масъала (бо тоҷикӣ)

Бо тағйирёбандаи ном ҷумларо чоп кунед: `Номи қаҳрамони мо <ном> аст!`

- f-сатрро истифода баред, масалан `f"... {name} ..."`.
- 🔗 Тағйирёбандаи `name`-ро аз **масъалаи 3** истифода баред.

### Example:
```
Output: Номи қаҳрамони мо Артур аст!
```

---

# Task 5: Stat Card 🟢

## English Description

Create **three** variables — name, health, gold — and print the hero's stat card.

- 🔗 Name from **Task 4** + two new variables. `health` and `gold` are needed later.

## Описание задачи (на русском)

Создайте **три** переменные — имя, здоровье, золото — и выведите карточку героя.

- 🔗 Имя из **задачи 4** + две новые переменные. `health` и `gold` понадобятся дальше.

## Тавсифи масъала (бо тоҷикӣ)

**Се** тағйирёбанда созед — ном, саломатӣ, тилло — ва варақаи қаҳрамонро чоп кунед.

- 🔗 Ном аз **масъалаи 4** + ду тағйирёбандаи нав. `health` ва `gold` баъдтар лозим мешаванд.

### Example:
```
Output:
Ном: Артур
Саломатӣ: 100
Тилло: 50
```

---

# Task 6: Ask the Name 🟢

## English Description

Instead of a fixed name, read it from the console with `input` and greet the hero.

- 🔗 Makes the greeting from **Task 4** interactive.

## Описание задачи (на русском)

Вместо готового имени считайте его с консоли через `input` и поприветствуйте героя.

- 🔗 Делает приветствие из **задачи 4** интерактивным.

## Тавсифи масъала (бо тоҷикӣ)

Ба ҷои номи тайёр, онро аз консол бо `input` хонед ва ба қаҳрамон хушомад гӯед.

- 🔗 Хушомади **масъалаи 4**-ро зинда мекунад.

### Example:
```
Input: Артур
Output: Хуш омадӣ, Артур!
```

---

# Task 7: Hero's Age 🟢

## English Description

Read the hero's age, convert it to a number with `int()`, and print it.

- Remember: `input()` returns text, so wrap it in `int()`.
- 🔗 Adds a number to the hero from **Task 6**.

## Описание задачи (на русском)

Считайте возраст героя, преобразуйте в число через `int()` и выведите его.

- Помните: `input()` возвращает текст, поэтому оборачивайте его в `int()`.
- 🔗 Добавляет число к герою из **задачи 6**.

## Тавсифи масъала (бо тоҷикӣ)

Синну соли қаҳрамонро хонед, онро бо `int()` ба адад табдил диҳед ва чоп кунед.

- Дар хотир доред: `input()` матн бармегардонад, бинобар ин онро дар `int()` печонед.
- 🔗 Ба қаҳрамони **масъалаи 6** адад илова мекунад.

### Example:
```
Input: 20
Output: Қаҳрамон 20-сола аст.
```

---

# Task 8: Birth Year 🟢

## English Description

From the age, calculate the birth year: `2026 - age`.

- 🔗 Uses the `age` variable from **Task 7**.

## Описание задачи (на русском)

По возрасту вычислите год рождения: `2026 - age`.

- 🔗 Использует переменную `age` из **задачи 7**.

## Тавсифи масъала (бо тоҷикӣ)

Аз рӯи синну сол соли таваллудро ҳисоб кунед: `2026 - age`.

- 🔗 Тағйирёбандаи `age`-ро аз **масъалаи 7** истифода мебарад.

### Example:
```
Input: 20
Output: Соли таваллуд: 2006
```

---

# Task 9: Two Stats 🟢

## English Description

Read **two** numbers — strength and health — and print both.

- 🔗 Prepares two numbers that you will add in **Task 10**.

## Описание задачи (на русском)

Считайте **два** числа — силу и здоровье — и выведите оба.

- 🔗 Готовит два числа, которые вы сложите в **задаче 10**.

## Тавсифи масъала (бо тоҷикӣ)

**Ду** адад хонед — қувва ва саломатӣ — ва ҳарду чоп кунед.

- 🔗 Ду ададеро тайёр мекунад, ки дар **масъалаи 10** ҷамъ мекунед.

### Example:
```
Input:
40
100
Output: Қувва = 40, Саломатӣ = 100
```

---

# Task 10: Total Power 🟢

## English Description

Read three numbers — strength, health, magic — add them, and print the hero's **total power**.

- 🔗 Extends **Task 9** and creates `power` — the **key** for the conditions block.

## Описание задачи (на русском)

Считайте три числа — силу, здоровье, магию — сложите их и выведите **общую мощь** героя.

- 🔗 Продолжает **задачу 9** и создаёт `power` — **ключ** для блока условий.

## Тавсифи масъала (бо тоҷикӣ)

Се адад хонед — қувва, саломатӣ, ҷоду — онҳоро ҷамъ кунед ва **қувваи умумии** қаҳрамонро чоп кунед.

- 🔗 **Масъалаи 9**-ро идома медиҳад ва `power`-ро месозад — **калиди** блоки шартҳо.

### Example:
```
Input:
40
100
30
Output: Қувваи умумии қаҳрамон: 170
```

---

# Task 11: Strong or Weak 🟡

## English Description

If power is greater than 100, print `Қаҳрамон қавӣ аст!`, otherwise print `Қаҳрамон заиф аст.`

- Use `if / else`.
- 🔗 Tests the `power` variable from **Task 10**.

## Описание задачи (на русском)

Если мощь больше 100, выведите `Қаҳрамон қавӣ аст!`, иначе выведите `Қаҳрамон заиф аст.`

- Используйте `if / else`.
- 🔗 Проверяет переменную `power` из **задачи 10**.

## Тавсифи масъала (бо тоҷикӣ)

Агар қувва аз 100 зиёд бошад, `Қаҳрамон қавӣ аст!`-ро чоп кунед, вагарна `Қаҳрамон заиф аст.`-ро.

- Аз `if / else` истифода баред.
- 🔗 Тағйирёбандаи `power`-ро аз **масъалаи 10** месанҷад.

### Example:
```
Input: 170
Output: Қаҳрамон қавӣ аст!
```
```
Input: 50
Output: Қаҳрамон заиф аст.
```

---

# Task 12: Purchase 🟡

## English Description

Compare the hero's gold with the sword's price (50): print `Ту метавонӣ шамшер харӣ!` if there is enough, otherwise `Тилло намерасад.`

- Use `if / else`.
- 🔗 Continues the `gold` idea from **Task 5**.

## Описание задачи (на русском)

Сравните золото героя с ценой меча (50): выведите `Ту метавонӣ шамшер харӣ!`, если хватает, иначе `Тилло намерасад.`

- Используйте `if / else`.
- 🔗 Развивает идею `gold` из **задачи 5**.

## Тавсифи масъала (бо тоҷикӣ)

Тиллои қаҳрамонро бо нархи шамшер (50) муқоиса кунед: агар кифоя бошад `Ту метавонӣ шамшер харӣ!`-ро, вагарна `Тилло намерасад.`-ро чоп кунед.

- Аз `if / else` истифода баред.
- 🔗 Идеяи `gold`-ро аз **масъалаи 5** идома медиҳад.

### Example:
```
Input: 50
Output: Ту метавонӣ шамшер харӣ!
```
```
Input: 30
Output: Тилло намерасад.
```

---

# Task 13: Hero's Level 🟡

## English Description

Based on power, print the hero's level: `Навкор` (< 100), `Ҷанговар` (100–199), or `Афсона` (200+).

- Use `if / elif / else`.
- 🔗 Reuses `power` from **Task 10** — now with **three** branches.

## Описание задачи (на русском)

По мощи выведите уровень героя: `Навкор` (< 100), `Ҷанговар` (100–199) или `Афсона` (200+).

- Используйте `if / elif / else`.
- 🔗 Снова использует `power` из **задачи 10** — теперь **три** ветки.

## Тавсифи масъала (бо тоҷикӣ)

Аз рӯи қувва дараҷаи қаҳрамонро чоп кунед: `Навкор` (< 100), `Ҷанговар` (100–199) ё `Афсона` (200+).

- Аз `if / elif / else` истифода баред.
- 🔗 Боз `power`-ро аз **масъалаи 10** истифода мебарад — акнун **се** роҳ.

### Example:
```
Input: 170
Output: Дараҷа: Ҷанговар
```
```
Input: 250
Output: Дараҷа: Афсона
```

---

# Task 14: Battle 🟡

## English Description

Read the power of the hero and the enemy, compare them, and print the battle result: `Ғалаба!`, `Мағлубият...`, or `Баробар!`

- Use `if / elif / else`.
- 🔗 Two powers (like **Task 10**) — now with an enemy.

## Описание задачи (на русском)

Считайте мощь героя и врага, сравните их и выведите итог битвы: `Ғалаба!`, `Мағлубият...` или `Баробар!`

- Используйте `if / elif / else`.
- 🔗 Две мощи (как в **задаче 10**) — теперь с врагом.

## Тавсифи масъала (бо тоҷикӣ)

Қувваи қаҳрамон ва душманро хонед, онҳоро муқоиса кунед ва натиҷаи набардро чоп кунед: `Ғалаба!`, `Мағлубият...` ё `Баробар!`

- Аз `if / elif / else` истифода баред.
- 🔗 Ду қувва (мисли **масъалаи 10**) — акнун бо душман.

### Example:
```
Input:
170
150
Output: Ғалаба!
```
```
Input:
120
120
Output: Баробар!
```

---

# Task 15: Allowed to Play 🟡

## English Description

Using `and`, check: if age is at least 12 **and** health is above 0, print `Бозӣ иҷозат аст!`, otherwise `Бозӣ иҷозат нест.`

- 🔗 Combines `age` from **Task 7** and `health` from **Task 9** in one condition.

## Описание задачи (на русском)

С помощью `and` проверьте: если возраст не меньше 12 **и** здоровье больше 0, выведите `Бозӣ иҷозат аст!`, иначе `Бозӣ иҷозат нест.`

- 🔗 Объединяет `age` из **задачи 7** и `health` из **задачи 9** в одном условии.

## Тавсифи масъала (бо тоҷикӣ)

Бо `and` санҷед: агар синну сол на камтар аз 12 **ва** саломатӣ аз 0 зиёд бошад, `Бозӣ иҷозат аст!`-ро, вагарна `Бозӣ иҷозат нест.`-ро чоп кунед.

- 🔗 `age`-ро аз **масъалаи 7** ва `health`-ро аз **масъалаи 9** дар як шарт муттаҳид мекунад.

### Example:
```
Input:
20
100
Output: Бозӣ иҷозат аст!
```
```
Input:
10
100
Output: Бозӣ иҷозат нест.
```

---

# Task 16: Countdown 🟡

## English Description

Using a `for` loop, count down from 3 to 1, then print `Оғоз!`

- 🔗 Your **first loop** — the countdown before the battles of **Task 14**.

## Описание задачи (на русском)

С помощью цикла `for` отсчитайте от 3 до 1, затем выведите `Оғоз!`

- 🔗 Ваш **первый цикл** — отсчёт перед битвами из **задачи 14**.

## Тавсифи масъала (бо тоҷикӣ)

Бо ҳалқаи `for` аз 3 то 1 ҳисоб кунед, баъд `Оғоз!`-ро чоп кунед.

- 🔗 **Аввалин ҳалқаи** шумо — ҳисоби баръакс пеш аз набардҳои **масъалаи 14**.

### Example:
```
Output:
3
2
1
Оғоз!
```

---

# Task 17: Five Enemies 🟡

## English Description

Using a `for` loop with `range`, print five enemies: `Душман №1` … `Душман №5`.

- 🔗 Repeats the enemy from **Task 14** five times.

## Описание задачи (на русском)

С помощью цикла `for` и `range` выведите пять врагов: `Душман №1` … `Душман №5`.

- 🔗 Повторяет врага из **задачи 14** пять раз.

## Тавсифи масъала (бо тоҷикӣ)

Бо ҳалқаи `for` ва `range` панҷ душманро чоп кунед: `Душман №1` … `Душман №5`.

- 🔗 Душмани **масъалаи 14**-ро панҷ маротиба такрор мекунад.

### Example:
```
Output:
Душман №1
Душман №2
Душман №3
Душман №4
Душман №5
```

---

# Task 18: Saving Gold 🟡

## English Description

Using a `while` loop, add 20 gold each day until you reach 100, and count the days.

- 🔗 Adds a loop to `gold` from **Task 12** — until it is enough.

## Описание задачи (на русском)

С помощью цикла `while` каждый день добавляйте 20 золота, пока не наберёте 100, и посчитайте дни.

- 🔗 Добавляет цикл к `gold` из **задачи 12** — пока не хватит.

## Тавсифи масъала (бо тоҷикӣ)

Бо ҳалқаи `while` ҳар рӯз 20 тилло ҷамъ кунед, то ба 100 расед, ва рӯзҳоро шуморед.

- 🔗 Ба `gold`-и **масъалаи 12** ҳалқа илова мекунад — то кифоя шавад.

### Example:
```
Output:
Рӯзи 1: тилло = 20
Рӯзи 2: тилло = 40
Рӯзи 3: тилло = 60
Рӯзи 4: тилло = 80
Рӯзи 5: тилло = 100
Ҳадаф дар 5 рӯз ба даст омад!
```

---

# Task 19: Damage Table 🟡

## English Description

Using a `for` loop, build a damage table for levels 1 to 10, where `damage = level * 5`.

- Output format for each line: `Дараҷа <level>: зарба <damage>`.
- 🔗 Turns the levels from **Task 13** into damage inside a loop.

## Описание задачи (на русском)

С помощью цикла `for` составьте таблицу урона для уровней от 1 до 10, где `damage = level * 5`.

- Формат каждой строки: `Дараҷа <level>: зарба <damage>`.
- 🔗 Превращает уровни из **задачи 13** в урон внутри цикла.

## Тавсифи масъала (бо тоҷикӣ)

Бо ҳалқаи `for` ҷадвали зарбаро барои дараҷаҳои 1 то 10 созед, ки дар он `damage = level * 5`.

- Формати ҳар сатр: `Дараҷа <level>: зарба <damage>`.
- 🔗 Дараҷаҳои **масъалаи 13**-ро дар дохили ҳалқа ба зарба табдил медиҳад.

### Example:
```
Output:
Дараҷа 1: зарба 5
Дараҷа 2: зарба 10
Дараҷа 3: зарба 15
Дараҷа 4: зарба 20
Дараҷа 5: зарба 25
Дараҷа 6: зарба 30
Дараҷа 7: зарба 35
Дараҷа 8: зарба 40
Дараҷа 9: зарба 45
Дараҷа 10: зарба 50
```

---

# Task 20: Sum of Points 🟡

## English Description

Read a number `n` and, using a `for` loop, sum all numbers from 1 to `n` (the total score).

- 🔗 Same accumulation trick as **Task 18** — now with numbers.

## Описание задачи (на русском)

Считайте число `n` и с помощью цикла `for` сложите все числа от 1 до `n` (сумма очков).

- 🔗 Тот же приём накопления, что и в **задаче 18** — теперь с числами.

## Тавсифи масъала (бо тоҷикӣ)

Ададеро `n` хонед ва бо ҳалқаи `for` ҳамаи ададҳоро аз 1 то `n` ҷамъ кунед (маҷмӯи холҳо).

- 🔗 Ҳамон усули ҷамъшавии **масъалаи 18** — акнун бо ададҳо.

### Example:
```
Input: 5
Output: Маҷмӯи холҳо: 15
```
```
Input: 10
Output: Маҷмӯи холҳо: 55
```

---

# Task 21: Inventory 🟡

## English Description

Create a **list** of items (`шамшер`, `сипар`, `маъҷун`) and print it.

- 🔗 Items from **Task 12** (the sword) are now gathered into a list.

## Описание задачи (на русском)

Создайте **список** предметов (`шамшер`, `сипар`, `маъҷун`) и выведите его.

- 🔗 Предметы из **задачи 12** (меч) теперь собраны в список.

## Тавсифи масъала (бо тоҷикӣ)

**Рӯйхати** ашё созед (`шамшер`, `сипар`, `маъҷун`) ва онро чоп кунед.

- 🔗 Ашёи **масъалаи 12** (шамшер) акнун дар як рӯйхат ҷамъ шудааст.

### Example:
```
Output: ['шамшер', 'сипар', 'маъҷун']
```

---

# Task 22: Add an Item 🟡

## English Description

Using `.append()`, add `камон` to the list and print the result.

- 🔗 Takes the list from **Task 21** and extends it.

## Описание задачи (на русском)

С помощью `.append()` добавьте `камон` в список и выведите результат.

- 🔗 Берёт список из **задачи 21** и расширяет его.

## Тавсифи масъала (бо тоҷикӣ)

Бо `.append()` ба рӯйхат `камон` илова кунед ва натиҷаро чоп кунед.

- 🔗 Рӯйхати **масъалаи 21**-ро мегирад ва васеъ мекунад.

### Example:
```
Output: ['шамшер', 'сипар', 'маъҷун', 'камон']
```

---

# Task 23: Loop the Inventory 🟡

## English Description

Using a `for` loop, go through the list and print each item on its own line as `- <item>`.

- 🔗 Merges the loop from **Task 17** with the list from **Task 22**.

## Описание задачи (на русском)

С помощью цикла `for` пройдите по списку и выведите каждый предмет на отдельной строке как `- <item>`.

- 🔗 Соединяет цикл из **задачи 17** со списком из **задачи 22**.

## Тавсифи масъала (бо тоҷикӣ)

Бо ҳалқаи `for` аз рӯйхат гузаред ва ҳар ашёро дар сатри алоҳида ҳамчун `- <item>` чоп кунед.

- 🔗 Ҳалқаи **масъалаи 17**-ро бо рӯйхати **масъалаи 22** муттаҳид мекунад.

### Example:
```
Output:
- шамшер
- сипар
- маъҷун
- камон
```

---

# Task 24: Count Items 🟡

## English Description

Using `len()`, count the number of items in the list and print it.

- 🔗 Counts the same list from **Task 23**.

## Описание задачи (на русском)

С помощью `len()` посчитайте количество предметов в списке и выведите его.

- 🔗 Считает тот же список из **задачи 23**.

## Тавсифи масъала (бо тоҷикӣ)

Бо `len()` шумораи ашёро дар рӯйхат ҳисоб кунед ва чоп кунед.

- 🔗 Ҳамон рӯйхати **масъалаи 23**-ро мешуморад.

### Example:
```
Output: Дар анбор 4 ашё ҳаст.
```

---

# Task 25: Final Report 🔴

## English Description

Put **everything** together. Read the hero's name and power, then print a full report: name, power, level, the item list, and the total number of items.

- 🔗 **Everything comes together:** `input` (**Task 6**) + `power` (**Task 10**) + level (**Task 13**) + list loop (**Task 23**) + `len` (**Task 24**).

## Описание задачи (на русском)

Соберите **всё** вместе. Считайте имя и мощь героя, затем выведите полный отчёт: имя, мощь, уровень, список предметов и общее количество предметов.

- 🔗 **Здесь всё объединяется:** `input` (**задача 6**) + `power` (**задача 10**) + уровень (**задача 13**) + цикл по списку (**задача 23**) + `len` (**задача 24**).

## Тавсифи масъала (бо тоҷикӣ)

**Ҳамаро** якҷоя кунед. Ном ва қувваи қаҳрамонро хонед, баъд ҳисоботи пурра чоп кунед: ном, қувва, дараҷа, рӯйхати ашё ва шумораи умумии ашё.

- 🔗 **Ин ҷо ҳама чиз муттаҳид мешавад:** `input` (**масъалаи 6**) + `power` (**масъалаи 10**) + дараҷа (**масъалаи 13**) + ҳалқа аз рӯйхат (**масъалаи 23**) + `len` (**масъалаи 24**).

### Example:
```
Input:
Артур
170
Output:
===== ҲИСОБОТИ ҚАҲРАМОН =====
Ном: Артур
Қувва: 170
Дараҷа: Ҷанговар
Анбор:
  - шамшер
  - сипар
  - маъҷун
Ҳамагӣ ашё: 3
```
