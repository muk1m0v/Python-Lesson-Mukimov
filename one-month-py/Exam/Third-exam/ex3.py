#-#! FOR EXAM 3 WEEK
#^ Вопрос-1: Дар бораи рекурсия нависед!
'''
Рекурсия - это функция которая позваляеть визывать самого себя 
в нутри функции и для того чтобы остановить цикл нам нужно сделать
return если не делать ретурн то цикл дойдёт до 996 раз и остоновиться
'''
#^ Вопрос-2: Closure(Замыкания) - ро бо мисолҳо фаҳмонед.
'''
Замыкание в Python — это функция, которая сохраняет состояние 
окружающей среды (например, переменные из внешней функции)
даже после её завершения выполнения.
Пример: Для обесния
-----------------
1.def plus(x):
2.  def off(y):
3.      print(x + y)
4.  return off
5.
6.a = plus(4)
7.a(3)
-----------------
Вот на примере код для сложение двух переменных:
Вот обесние каждой строки кода от 1 до 7:
1: Создаёться функция которая принимаеть одну переменную x
2: Это уже внутренная функция которая тоже берёт одну переменную y
3: Тут в это строке кода нам показываеть что мы виходим значение x(4) + y(3) = 7
4: А в этой строке мы возврашаем наш результать от сложение
5: Пробел
6: А тут мы даём функцию значение а переменную берём значение каторое
- Сделалы ретурн для того чтобы отправить данные в переменую
7: a(3) равно как off(3) 
'''
#^ Вопрос-3: Контейнерҳоро ба хотир оварда онҳоро нависед.
'''
Контейнеры в Python бывают нескольких типов:

1. Кортежи (tuple) – неизменяемый порядковый контейнер:
    t = (1, 2, 'a')

2. Списки (list) – изменяемый порядковый контейнер:
    l = [1, 2, 'a']  #? Можно изменить элементы

3. Множества (set) – неупорядоченный неизменяемый набор уникальных элементов:
    s = {1, 2, 3}  #! Не допускает дубликатов

4. Словари (dict) – ассоциативные массивы с ключами и значениями:
    d = {'a': 1, 'b': 2}
'''
#^ Вопрос-4: Дар бораи lambda expression ва list comprehension нависед.
'''
List comprehension — синтаксис для создания списков на основе итерируемого объекта:
squares = [x**2 for x in range(5)]  # [0, 1, 4, 9, 16]

Dict comprehension — аналогично, но для словарей:
mapping = {x: x**2 for x in range(3)}  # {'0': 0, '1': 1, '2': 4}
'''
#^ Вопрос-5: *args ва **kwargs барои чи лозиманд ва чигуна онҳоро истифода мебарем.
'''
*args — позволяет передавать количество аргументов произвольного типа функцией, как кортеж. Например:
def пример(аргументы):
    print(аргументы)
пример(*[1, 2, 'text', True])
----------------
**kwargs — передаёт именованные параметры в виде словаря. Пример:
python
def функция(**data):
    for key, value in data.items():
        print(f"{key}: {value}")

функция(имя="Али", возраст=30)
'''
#todo: -------------------------------------------------
#? TASK 1: Напишите программу на Python, заменяющую каждый символ слова длиной пять и более символом решетки (#).
# def replace_words(word):
#     return '#' * len(word) if len(word.lower()) >= 5 else word

# text = str(input('Enter the text: '))
# words = text.split()
# result = [replace_words(word) for word in words]
# print(' '.join(result))
#? TASK 2: Напишите фунцию Python для вставки элемента в указанную позицию в заданный список. [1, 1, 2, 3, 4, 4, 5, 1]
# def insert_element(lst, element, index):
#     lst.insert(index, element)
#     return lst.copy()

# lst = [1, 1, 2, 3, 4, 4, 5, 1]
# result = insert_element(lst, input('Text: '), int(input('Index: ')))
# print(result) 
#? TASK 3: Создайте программу, которая конвертирует валюту одного номинала в другой.
# def convert_currency(amount, to_rub=8.33, to_usd=0.094, to_eur=0.084, uz_sum=1000):
#     result = {}
#     tjs_to_rub = amount * to_rub
#     tjs_to_usd = amount * to_usd
#     tjs_to_eur = amount * to_eur
#     tjs_to_uzb = amount * uz_sum
#     result['RUB'] = tjs_to_rub
#     result['USD'] = tjs_to_usd
#     result['EUR'] = tjs_to_eur
#     result['UZ_SUM'] = tjs_to_uzb
#     return result

# amount = int(input("Enter the amount in TJS: "))
# convert = convert_currency(amount)
# for currency, value in convert.items():
#     print(f"{currency} -> {value}")
#? TASK 4: Напишите фунцию на Python для преобразования списка из нескольких целых чисел в одно целое число.
# def concatenate_list_to_number(numbers):
#     return int(''.join(map(str, numbers)))

# numbers = input('Enter: ').split()
# result = concatenate_list_to_number(numbers)
# print(result)
#? TASK 5: Создайте замыкание, которое объединяет заданную строку с другой строкой.
# def hello():
#     def say(name):
#         print(f'Salom {name}')
#     return say

# a = hello()
# a('Muhammad')
# a('Abbos')
# a('Aki Khairiddin')
#? TASK 6: Напишите рекурсивную функцию для поиска минимального элемента в списке.
# def find_min(ok, index=0):
#     if index == len(ok):
#         return ok[index - 1]
#     if ok[index] < find_min(ok, index + 1):
#         return ok[index]
#     else:
#         return find_min(ok, index + 1)

# result = find_min(input('Введите числа: ').split())
# print(f"Минимальный элемент: {result}")
#? TASK 7: Дано натуральное число n, вычислите сумму 1^2+2^2+...+n^2. Создайте функцию, которая принимает одно натуральное число n. Вам нужно вывести вычисленную сумму. Не забудьте вернуть результат. Решите эту задачу с помощью функции рекурсии.
# def sum_squares_recursive(n):
#     if n == 1:
#         return 1
#     else:
#         return n**2 + sum_squares_recursive(n - 1)
# print(sum_squares_recursive(int(input('Enter the number: ')))) 
#? TASK 8: Дана строка, содержащая только английские буквы (большие и маленькие). Добавить открывающиеся и закрывающиеся скобки по следующему образцу: "example" -> "e(x(a(m)p)l)e" (До середины добавлены открывающиеся скобки, после середины – закрывающиеся. В случае, когда длина строки четна в скобках, расположенных в середине, должно быть 2 символа. ("card -> c(ar)d", но не "c(a()r)d"). Решите эту задачу с помощью функции рекурсии.
#? TASK 9: Напишите функцию на Python для печати словаря, в котором ключами являются числа от 1 до N (оба включены), а значениями являются квадраты ключей.
# def print_squares_dictionary(n):
#     result = {i: i ** 2 for i in range(1, n + 1)}
#     print(result)

# print_squares_dictionary(int(input('Enter the num: ')))
#? TASK 10: Дан список элементов любых типов данных. Создайте функцию на Python для разделения элементов по их типам и сохранения их в новый словарь. Ключи словаря должны иметь тип данных, а его элементом должны быть данные, принадлежащие этому типу.
#! УСПЕТ НАКАДИМ